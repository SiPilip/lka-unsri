
import React from 'react';

// This is a placeholder component to fix a build issue caused by an empty file.
// It is not actively used in the application's logic but prevents the bundler from crashing.
const Dashboard: React.FC = () => {
  return null;
};

export default Dashboard;
